import{fireEvent,render,screen,within}from"@testing-library/react";import Home from"@/app/page";import{LocaleProvider}from"@/i18n/locale-provider";
const renderHome=()=>render(<LocaleProvider><Home/></LocaleProvider>);
describe("finance home",()=>{
beforeEach(()=>{localStorage.clear();delete process.env.NEXT_PUBLIC_MAINTENANCE_MODE});
it("always renders the finance home and never shows a maintenance page",()=>{process.env.NEXT_PUBLIC_MAINTENANCE_MODE="true";renderHome();expect(screen.queryByRole("heading",{name:"Service maintenance"})).not.toBeInTheDocument();expect(screen.queryByText(/temporarily offline/i)).not.toBeInTheDocument();expect(screen.getByTestId("desktop-home")).toBeInTheDocument();expect(screen.getByTestId("mobile-home")).toBeInTheDocument();expect(screen.getByTestId("top-market-ticker")).toBeInTheDocument()});
it("uses separate responsive layouts with English as the default",()=>{renderHome();const desktop=screen.getByTestId("desktop-home"),mobile=screen.getByTestId("mobile-home");expect(desktop).toHaveClass("hidden","lg:block");expect(mobile).toHaveClass("lg:hidden");expect(screen.getByRole("heading",{name:"Blockchain Savings"})).toBeInTheDocument();expect(within(mobile).getByLabelText("Mobile navigation")).toHaveClass("fixed");expect(document.querySelectorAll("#home")).toHaveLength(1);expect(document.querySelectorAll("#features")).toHaveLength(1)});
it("keeps rates numeric and removes annual-rate content",()=>{renderHome();expect(screen.queryByText(/Annual Rate|APR|年化收益/)).not.toBeInTheDocument();for(const layout of[screen.getByTestId("desktop-home"),screen.getByTestId("mobile-home")]){const tables=within(layout).getAllByTestId("rate-table");expect(tables).toHaveLength(2);expect(within(tables[0]).getByRole("heading",{name:/Flexible Savings Rates/})).toBeInTheDocument();expect(within(tables[1]).getByRole("heading",{name:/Fixed Savings Rates/})).toBeInTheDocument();expect(within(tables[0]).getAllByRole("columnheader")).toHaveLength(2);expect(within(tables[0]).getByText("2.70%")).toBeInTheDocument()}});
it("keeps the promo carousel above the global volume globe and keeps all advantages",()=>{renderHome();const promoSlides=screen.getAllByTestId("promo-banner-slide");expect(promoSlides).toHaveLength(4);expect(screen.queryByTestId("desktop-global-volume-globe")).not.toBeInTheDocument();expect(screen.queryByTestId("mobile-global-volume-globe")).not.toBeInTheDocument();const heroGlobe=screen.getByTestId("hero-global-volume-globe");expect(promoSlides[0].compareDocumentPosition(heroGlobe)&Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();expect(heroGlobe).not.toHaveClass("panel");expect(heroGlobe).toHaveClass("global-volume-hero");expect(heroGlobe).toHaveTextContent("Blockchain Savings");expect(heroGlobe).toHaveTextContent("24H Volume Flow");expect(heroGlobe).toHaveTextContent("Flexible USDC Access");expect(heroGlobe).toHaveTextContent("Fixed-Term Yield Plan");expect(within(heroGlobe).getAllByTestId("globe-proof-ribbon-item").length).toBeGreaterThan(4);for(const layout of[screen.getByTestId("desktop-home"),screen.getByTestId("mobile-home")]){const panel=within(layout).getByTestId("advantages-panel");const firstButton=within(panel).getByRole("button",{name:/01.*Rewards Program/});expect(within(panel).getAllByRole("button")).toHaveLength(11);fireEvent.click(firstButton);expect(within(panel).getByTestId(`${layout.dataset.testid==="desktop-home"?"desktop":"mobile"}-advantage-body-01`).textContent).toBeTruthy()}});
it("applies the illuminated ticker treatment before the promo carousel and hero globe",()=>{renderHome();const topTicker=screen.getByTestId("top-market-ticker");const promoSlide=screen.getAllByTestId("promo-banner-slide")[0];const heroGlobe=screen.getByTestId("hero-global-volume-globe");expect(topTicker).toHaveClass("market-ticker-highlight","market-ticker-spotlight","market-ticker-top");expect(topTicker.compareDocumentPosition(promoSlide)&Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();expect(promoSlide.compareDocumentPosition(heroGlobe)&Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();expect(within(topTicker).getAllByTestId("ticker-value-pill").length).toBeGreaterThan(0);expect(within(topTicker).getAllByTestId("ticker-change-positive").length).toBeGreaterThan(0);expect(within(topTicker).getAllByTestId("ticker-change-negative").length).toBeGreaterThan(0)})
it("keeps mobile advantages unchanged while refining the desktop advantages panel",()=>{renderHome();const desktopPanel=within(screen.getByTestId("desktop-home")).getByTestId("advantages-panel");const mobilePanel=within(screen.getByTestId("mobile-home")).getByTestId("advantages-panel");expect(desktopPanel).toHaveClass("advantages-panel-desktop");expect(mobilePanel).not.toHaveClass("advantages-panel-desktop");const firstButton=within(desktopPanel).getByRole("button",{name:/01.*Rewards Program/});expect(firstButton).toHaveClass("advantage-row");fireEvent.click(firstButton);expect(firstButton).toHaveClass("advantage-row-active");expect(within(desktopPanel).getByTestId("desktop-advantage-body-01")).toHaveClass("advantage-body-active")})
it("shows the full desktop advantages list without changing mobile advantages",()=>{renderHome();const desktop=screen.getByTestId("desktop-home");const sidebar=within(desktop).getByTestId("desktop-sidebar");const desktopPanel=within(sidebar).getByTestId("advantages-panel");const mobilePanel=within(screen.getByTestId("mobile-home")).getByTestId("advantages-panel");expect(desktopPanel).toHaveClass("advantages-panel-desktop","advantages-panel-complete");expect(within(desktopPanel).getAllByRole("button")).toHaveLength(11);expect(within(sidebar).getByRole("button",{name:/11.*How to join/})).toBeInTheDocument();expect(mobilePanel).not.toHaveClass("advantages-panel-complete")});
it("integrates a desktop-only on-chain status footer inside the advantages sidebar",()=>{renderHome();const sidebar=within(screen.getByTestId("desktop-home")).getByTestId("desktop-sidebar");const panel=within(sidebar).getByTestId("advantages-panel");const visual=within(panel).getByTestId("chain-flow-card");expect(panel).toContainElement(visual);expect(visual.compareDocumentPosition(within(panel).getByRole("button",{name:/11.*How to join/}))&Node.DOCUMENT_POSITION_PRECEDING).toBeTruthy();expect(within(screen.getByTestId("mobile-home")).queryByTestId("chain-flow-card")).not.toBeInTheDocument()});
it("keeps a desktop savings snapshot below the advantages and stretches it through the rates section",()=>{renderHome();const sidebar=within(screen.getByTestId("desktop-home")).getByTestId("desktop-sidebar");const snapshot=within(sidebar).getByTestId("savings-snapshot");expect(sidebar).toHaveClass("flex","flex-col","gap-3");expect(snapshot).toHaveClass("flex-1");expect(snapshot).toHaveTextContent("Savings snapshot");expect(within(snapshot).getByRole("link",{name:"Compare plans"})).toHaveAttribute("href","/pool?tab=plan");expect(within(screen.getByTestId("mobile-home")).queryByTestId("savings-snapshot")).not.toBeInTheDocument()});
it("labels both savings types and provides the same interest calculator on PC and H5",()=>{renderHome();const desktop=screen.getByTestId("desktop-home"),mobile=screen.getByTestId("mobile-home");const snapshot=within(desktop).getByTestId("savings-snapshot");expect(within(snapshot).getByTestId("flexible-rate-label")).toHaveTextContent("Flexible Savings Pool");expect(within(snapshot).getByTestId("fixed-rate-label")).toHaveTextContent("Fixed Savings Plan");const mobileRates=within(mobile).getByTestId("mobile-rates");const mobileCalculator=within(mobile).getByTestId("mobile-savings-calculator");expect(mobileRates.compareDocumentPosition(mobileCalculator)&Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();for(const calculator of[within(snapshot).getByTestId("desktop-savings-calculator"),mobileCalculator]){const amount=within(calculator).getByRole("spinbutton",{name:"Deposit amount"});fireEvent.change(amount,{target:{value:"10000"}});expect(calculator).toHaveTextContent("0.90%");expect(calculator).toHaveTextContent("90.00 USDC");fireEvent.click(within(calculator).getByRole("button",{name:"Fixed savings"}));fireEvent.change(amount,{target:{value:"50000"}});fireEvent.change(within(calculator).getByRole("combobox",{name:"Fixed term"}),{target:{value:"30"}});expect(calculator).toHaveTextContent("2.20%");expect(calculator).toHaveTextContent("1,100.00 USDC");expect(calculator).toHaveTextContent(/paid once at maturity/i);expect(calculator).toHaveTextContent(/estimate only/i)}});
it("keeps desktop action blocks visible and clickable outside the advantages sidebar",()=>{renderHome();const desktop=screen.getByTestId("desktop-home");const actionRail=within(desktop).getByTestId("desktop-action-rail");const quick=within(actionRail).getByTestId("quick-actions");expect(within(quick).getAllByRole("link")).toHaveLength(4);expect(within(quick).getByRole("link",{name:/Mystery Box/})).toHaveAttribute("href","/docs");expect(within(quick).getByRole("link",{name:/Fixed Savings/})).toHaveAttribute("href","/pool?tab=plan")});
it("uses distinct functional destinations for the top navigation and feature actions",()=>{renderHome();const mainNav=screen.getByRole("navigation",{name:"Main navigation"});expect(within(mainNav).getByRole("link",{name:"Trade"})).toHaveAttribute("href","/#features");expect(within(mainNav).getByRole("link",{name:"Pool"})).toHaveAttribute("href","/pool");expect(within(mainNav).getByRole("link",{name:"Finance"})).toHaveAttribute("href","/loan");expect(within(mainNav).getByRole("link",{name:"Mystery Box"})).toHaveAttribute("href","/#mystery-box");expect(within(mainNav).getByRole("link",{name:"Invite"})).toHaveAttribute("href","/#invite-friends");expect(screen.getByRole("link",{name:"ReceiveVoucher"})).toHaveAttribute("href","/pool?tab=account");expect(document.querySelector("#mystery-box")).toBeInTheDocument();expect(document.querySelector("#invite-friends")).toBeInTheDocument();const mobileNav=screen.getByRole("navigation",{name:"Mobile navigation"});expect(within(mobileNav).getByRole("link",{name:/Trade/})).toHaveAttribute("href","/#features");expect(within(screen.getByTestId("desktop-home")).getAllByRole("link",{name:/Draw Now|Invite Now|Join Now/})).toHaveLength(3);expect(within(screen.getByTestId("desktop-home")).getAllByRole("link",{name:/Mystery Box|Invite Friends|Liquidity Mining|Fixed Savings/})).toHaveLength(4)});
it("opens distinct advantage bodies after switching every locale",()=>{renderHome();const language=screen.getByRole("combobox",{name:"Language"});for(const locale of["en","zh-CN","zh-TW","ja","ko","th"]){fireEvent.change(language,{target:{value:locale}});const panel=within(screen.getByTestId("desktop-home")).getByTestId("advantages-panel");const first=within(panel).getAllByRole("button")[0];fireEvent.click(first);const firstBody=within(panel).getByTestId("desktop-advantage-body-01").textContent;fireEvent.click(within(panel).getAllByRole("button")[1]);const secondBody=within(panel).getByTestId("desktop-advantage-body-02").textContent;expect(firstBody).toBeTruthy();expect(secondBody).toBeTruthy();expect(firstBody).not.toBe(secondBody)}});
it("switches advantage body copy with the selected locale",()=>{renderHome();const language=screen.getByRole("combobox",{name:"Language"});const panel=within(screen.getByTestId("desktop-home")).getByTestId("advantages-panel");fireEvent.change(language,{target:{value:"en"}});fireEvent.click(within(panel).getAllByRole("button")[0]);const englishBody=within(panel).getByTestId("desktop-advantage-body-01").textContent;expect(englishBody).toMatch(/eligible customers/i);fireEvent.change(language,{target:{value:"ja"}});const japaneseBody=within(panel).getByTestId("desktop-advantage-body-01").textContent;expect(japaneseBody).toBeTruthy();expect(japaneseBody).not.toBe(englishBody)});
it("emphasizes the activity cards without changing their responsive grid",()=>{renderHome();for(const layout of[screen.getByTestId("desktop-home"),screen.getByTestId("mobile-home")]){const grid=within(layout).getByTestId("feature-grid");expect(grid).toHaveClass("grid","gap-3","sm:grid-cols-2","lg:grid-cols-3");expect(within(grid).getByTestId("feature-card-primary")).toHaveClass("activity-card-primary");expect(within(grid).getAllByTestId("feature-card-secondary")).toHaveLength(2);expect(within(grid).getByText("8888 USDC")).toHaveClass("activity-reward-primary")}})
it("switches both PC and H5 content from the shared selector",()=>{renderHome();fireEvent.change(screen.getByRole("combobox",{name:"Language"}),{target:{value:"ja"}});expect(screen.getAllByText("フレキシブル貯蓄金利")).toHaveLength(2);expect(screen.getAllByText("選ばれる理由")).toHaveLength(2);expect(screen.getByRole("link",{name:"ホーム"})).toHaveAttribute("href","/")});
it("opens the mobile menu and exposes all six languages",()=>{renderHome();fireEvent.click(screen.getByRole("button",{name:"Open mobile menu"}));const drawer=screen.getByTestId("mobile-drawer");expect(drawer.parentElement).toBe(document.body);for(const label of["EN","简体","繁体","日本語","한국어","ไทย"])expect(within(drawer).getByRole("button",{name:label})).toBeInTheDocument();fireEvent.click(within(drawer).getAllByRole("button",{name:"Close mobile menu"})[1]);expect(screen.queryByTestId("mobile-drawer")).not.toBeInTheDocument()});
});
