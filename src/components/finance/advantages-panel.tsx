"use client";import{useState}from"react";import{advantages}from"@/constants/finance";import{useLocale}from"@/i18n/locale-provider";import{getFinanceCopy}from"@/i18n/finance-copy";
export function AdvantagesPanel({anchorId,instance,className=""}:{anchorId?:string;instance:"desktop"|"mobile";className?:string}){const[openId,setOpenId]=useState<string|null>(null);const{locale}=useLocale();const c=getFinanceCopy(locale);return <section id={anchorId} data-testid="advantages-panel" className={`panel overflow-hidden ${className}`}><div className="border-b border-line p-5"><p className="text-xs font-semibold tracking-[0.22em] text-warning">WHY BLOCKCHAIN SAVINGS</p><h2 className="mt-2 text-2xl font-semibold">{c.sections.advantages}</h2></div><div className="divide-y divide-line">{advantages.map((x,i)=>{const expanded=openId===x.id;return <div key={x.id}><button type="button" aria-expanded={expanded} aria-controls={`${instance}-advantage-${x.id}`} onClick={()=>setOpenId(expanded?null:x.id)} className="flex w-full items-center gap-3 px-5 py-4 text-left hover:bg-surface-soft"><span className="font-semibold text-warning">{x.id}.</span><span className="min-w-0 flex-1 font-medium">{c.advantages[i]}</span><span aria-hidden="true" className={`text-xl text-muted transition-transform ${expanded?"rotate-180":""}`}>⌄</span></button>{expanded&&<div id={`${instance}-advantage-${x.id}`} className="space-y-3 bg-canvas/45 px-5 pb-5 text-sm leading-7 text-muted"><p>{c.advantageBody}</p></div>}</div>})}</div></section>}
