"use client";
import {featureCards} from "@/constants/finance";import {GlowIcon} from "@/components/ui/glow-icon";import {useLocale} from "@/i18n/locale-provider";import {getFinanceCopy} from "@/i18n/finance-copy";
export function FeatureCards({anchorId}:{anchorId?:string}){const{locale}=useLocale();const c=getFinanceCopy(locale).features;return <section id={anchorId} data-testid="feature-grid" className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{featureCards.map((card,i)=>{const x=c[i];return <article key={card.icon} className="panel panel-hover relative flex min-h-52 overflow-hidden p-5 lg:min-h-48 lg:p-4"><div className="relative z-10 flex min-w-0 flex-1 flex-col"><h2 className="font-semibold">{x[0]}</h2><p className="mt-1 text-xs text-muted">{x[1]}</p><div className="mt-4"><p className="text-xs text-muted">{x[2]}</p><p className={`mt-1 text-xl font-medium ${card.tone==="orange"?"text-warning":"text-ink"}`}>{x[3]}</p></div><button className="primary-button mt-auto w-fit rounded-control px-4 py-2 text-sm font-medium">{x[4]}</button></div><GlowIcon name={card.icon} tone={card.tone} label={x[0]} size="xl" className="absolute -bottom-3 -right-3 rotate-6 opacity-80 lg:size-20"/></article>})}</section>}
